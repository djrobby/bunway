{
  "name": "local-package-smoke",
  "private": true,
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "bunway dev",
    "build": "bun run --bun --cwd web build",
    "test": "bun test",
    "typecheck": "bunx --bun tsc --noEmit && bun run --bun --cwd web check"
  },
  "dependencies": {
    "@bunway/cli": "file:../packages/cli",
    "@bunway/core": "file:../packages/core",
    "@elysiajs/cors": "latest",
    "@elysiajs/eden": "latest",
    "drizzle-orm": "latest",
    "elysia": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "latest",
    "pg": "8.23.0",
    "typescript": "latest"
  }
}
