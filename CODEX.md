# Codex Adapter

Read `START.md` and `AGENTS.md` first. `AGENTS.md` is authoritative. Project Codex defaults live in `.codex/config.toml`. Work autonomously inside the workspace. Never chain shell commands. Persist reusable human decisions into authoritative docs so future sessions do not ask again.
